Pill trạng thái vị trí OOH (chấm + nhãn) và tag loại biển/ngành hàng.

```jsx
<StatusPill status="available" /> // ● Còn trống (xanh)
<StatusPill status="soon" />      // ● Sắp hết (amber)
<StatusPill status="booked" />    // ● Đã đặt (xám — không đỏ)
<TypeBadge>Billboard</TypeBadge>
```
